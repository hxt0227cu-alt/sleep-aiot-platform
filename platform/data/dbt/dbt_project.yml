name: sleep_warehouse
version: 1.0.0
config-version: 2
profile: sleep_clickhouse
model-paths: ["models"]
test-paths: ["tests"]
clean-targets: ["target", "dbt_packages"]
models:
  sleep_warehouse:
    ods:
      +schema: ods
      +materialized: view
    dwd:
      +schema: dwd
      +materialized: incremental
    dws:
      +schema: dws
      +materialized: incremental
    ads:
      +schema: ads
      +materialized: table
