# 设计规范入口

本文件原为设计工具生成的空模板，不作为项目规范来源。

当前工程与设计约束请按以下顺序读取：

1. `AGENTS.md` 与 `.harness/rules/`：仓库工程治理规则。
2. `06-Figma版本与业务基线管理.md`：设计快照与产品代码的边界。
3. `src/styles.css`：Web 客户端当前样式事实。
4. `miniprogram/src/styles/`：微信小程序当前样式变量与混入。

本地可能存在的 `ui12.0/guidelines/Guidelines.md` 属于被 Git 忽略的设计归档，可作视觉参考，但不是产品或 CI 的权威入口。
