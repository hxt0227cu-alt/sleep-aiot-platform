name: Tmp Secret Guard Test
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Guarded
        if: ${{ secrets.NOPE != '' }}
        run: echo "guarded should not run"
      - name: Always
        run: echo "always runs"
