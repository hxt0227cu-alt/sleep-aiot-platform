{
  "name": "sleep-telemetry-ingest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/*.spec.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "kafkajs": "^2.2.4",
    "mqtt": "^5.15.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^7.0.2"
  },
  "pnpm": {
    "overrides": {
      "fast-uri": "3.1.6",
      "ip-address": "10.4.0"
    }
  }
}
