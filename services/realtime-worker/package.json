{
  "name": "sleep-realtime-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/*.spec.js",
    "test:rebalance": "node test/rebalance-smoke.cjs"
  },
  "dependencies": {
    "@clickhouse/client": "^1.11.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "kafkajs": "^2.2.4",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "overrides": {
      "fast-uri": "3.1.5"
    }
  }
}
